MEMORY {
	ZP:     start = $0000, size = $0100, type = rw, fill = no, define = yes;
	STACK:  start = $0100, size = $0100, type = rw, fill = no, define = yes;
	RAM:    start = $0200, size = $3E00, type = rw, fill = no;
	EEPROM: start = $8000, size = $8000, type = ro, fill = yes, file = "bios_eeprom.bin";
}

SEGMENTS {
  IRQ:          load = EEPROM, type = ro;
  RESET:        load = EEPROM, type = ro, start = $8100;
  HALT:         load = EEPROM, type = ro, start = $8200;
  PUT_CHAR:     load = EEPROM, type = ro, start = $8220;
  PUT_STRING:   load = EEPROM, type = ro, start = $8240;
  MEMCPY:       load = EEPROM, type = ro, start = $8260;
  GET_RANDOM_NUMBER: load = EEPROM, type = ro, start = $9000;
  SYSTEM_MISC:  load = EEPROM, type = ro, start = $D000;
  RODATA:       load = EEPROM, type = ro, start = $E000;
  WOZMON:       load = EEPROM, type = ro, start = $FF00;
  VECTOR_TABLE: load = EEPROM, type = ro, start = $FFFA;
}